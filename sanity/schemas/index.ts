// Import all schema types
import product from './product'
import service from './service'
import portfolio from './portfolio'

// Export all schema types
export const schemaTypes = [product, service, portfolio]
